[project]
name = "model-prices"
version = "0.1.0"
description = "Tracks LLM API pricing over time and renders an interactive price history chart, published via GitHub Pages."
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "plotly>=6.9.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
